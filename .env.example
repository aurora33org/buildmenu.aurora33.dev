# ============================================
# DATABASE (PostgreSQL)
# ============================================

# --- Local Development (docker-compose) ---
DATABASE_URL="postgresql://menuadmin:menupass@localhost:5434/menudb"
DIRECT_URL="postgresql://menuadmin:menupass@localhost:5434/menudb"

# --- Dokploy (PostgreSQL service interno) ---
# DATABASE_URL="postgresql://USER:PASSWORD@postgres:5432/DATABASE"
# DIRECT_URL="postgresql://USER:PASSWORD@postgres:5432/DATABASE"
# (ambas son iguales, sin pgbouncer)

# --- Vercel + Supabase ---
# DATABASE_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres:PASSWORD@db.xxxx.supabase.co:5432/postgres"
# (DATABASE_URL usa Transaction mode con pooling, DIRECT_URL es conexion directa para migrations)

# ============================================
# AUTH & SECURITY
# ============================================

# Secret key for session encryption (minimum 32 characters)
# Generate with: openssl rand -base64 32
SESSION_SECRET="your-super-secret-session-key-change-this-in-production-min-32-chars"

# Session expiration time in milliseconds (default: 7 days)
SESSION_MAX_AGE=604800000

# ============================================
# APPLICATION
# ============================================

NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Menu Create"

# ============================================
# SUPER ADMIN (for database seeding)
# ============================================

# These credentials will be used by `npm run db:seed`
# ⚠️ CHANGE THESE IN PRODUCTION!
SUPER_ADMIN_EMAIL="admin@menu.local"
SUPER_ADMIN_PASSWORD="admin123"

# ============================================
# OPTIONAL FEATURES
# ============================================

ENABLE_ANALYTICS=true
